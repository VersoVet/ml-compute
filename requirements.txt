# Ray cluster orchestration
ray[default,serve]==2.35.0
ray[tune]==2.35.0

# FastAPI wrapper
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP client (async)
httpx==0.25.0

# Monitoring & logging
python-json-logger==2.0.7

# Note: ML dependencies (torch, torchvision, ultralytics) are installed
# separately on worker nodes, not on the Ray head node orchestrator
# Note: onyx-sdk is installed from the global venv (/opt/onyx/venv)
